<%= form_for(@banner, :html => { :multipart => true }) do |f| %>
  <% if @banner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@banner.errors.count, "error") %> prohibited this banner from being saved:</h2>

      <ul>
      <% @banner.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :link %><br />
    <%= f.text_field :link %>
  </div>
  <div class="field">
    <%= f.label :img, "Imagem" %><br />
    <%= f.file_field :img %>
  </div>
  <div class="field">
    <%= f.label :displaying %><br />
    <%= f.check_box :displaying %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
